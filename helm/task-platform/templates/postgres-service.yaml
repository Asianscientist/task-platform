apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: {{ .Values.namespace }}
spec:
  type: {{ .Values.postgres.service.type }}
  clusterIP: None  
  selector:
    app: postgres
    role: db
  ports: 
    - name: postgres
      port: {{ .Values.postgres.service.port }}
      targetPort: postgres

apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: task-platform
spec:
  type: ClusterIP
  clusterIP: None  
  selector:
    app: postgres
    role: db
  ports: 
    - name: postgres
      port: 5432
      targetPort: postgres

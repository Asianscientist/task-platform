apiVersion: v1
kind: Service
metadata:
  name: worker
  namespace: task-platform
spec:
  type: ClusterIP
  selector: 
    app.kubernetes.io/name: worker
  ports:
    - name: metrics
      port: 8001
      targetPort: metrics